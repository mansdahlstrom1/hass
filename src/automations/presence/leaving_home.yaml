id: leaving_home
alias: Leaving Home
trigger:
- platform: state
  entity_id: binary_sensor.anyone_home
  from: 'on'
  to: 'off'

action:
  - service: homeassistant.turn_off
    entity_id:
      - light.dimmable_lights
      - group.my_switches
